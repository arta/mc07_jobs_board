%h1= @job.title

%p= @job.description
%p= @job.company
%p= @job.url

=link_to 'Home', jobs_path
=link_to 'Edit', [:edit, @job]
=link_to 'Delete', @job, method: :delete, data:{ confirm:'Are you sure?' }
